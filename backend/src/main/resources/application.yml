spring:
  security:
    user:
      name: rare
      password: f01a7031fc17

  data:
    elasticsearch:
        cluster:
          name: es-rare
        host: 127.0.0.1
        port: 9300

management:
  endpoint:
    health:
      show-details: 'always'
  endpoints:
    web:
      exposure:
        include: '*'

server:
  compression:
    enabled: true
    mime-types:
      - application/json
      - application/javascript
      - text/html
      - text/css

logging.level:
  root: ERROR
  org.springframework:
    boot.web.embedded.tomcat.TomcatWebServer: INFO
    web.client.RestTemplate: DEBUG

---
spring:
  profiles: beta

---
spring:
  profiles: dev

---
spring:
  profiles: rare-app
  cloud.config.name: rare

rare:
  elasticsearch-prefix: 'rare-'
  resource-dir: /tmp/rare/resources

server:
  servlet:
    context-path: /rare

---
spring:
  profiles: wheatis-app
  cloud.config.name: wheatis

rare:
  elasticsearch-prefix: 'wheatis-'
  resource-dir: /tmp/wheatis/resources

server:
  servlet:
    context-path: /wheatis
